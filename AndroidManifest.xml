<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pulseisland">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".PulseIslandApplication"
        android:icon="@drawable/ic_launcher"
        android:label="脉搏岛"
        android:theme="@style/Theme.DarkGlass">

        <service
            android:name=".core.IslandService"
            android:enabled="true"
            android:exported="false" />

        <activity
            android:name=".ui.MainHomeActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.DisclaimerActivity"
            android:exported="false"
            android:theme="@style/Theme.DarkGlass" />

        <activity
            android:name=".ui.LockScreenOverlayActivity"
            android:theme="@style/Theme.Transparent"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:launchMode="singleInstance"
            android:showWhenLocked="true"
            android:turnScreenOn="true" />

    </application>
</manifest>
<service
    android:name=".core.IslandNotificationListener"
    android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
    android:exported="true">
    <intent-filter>
        <action android:name="android.service.notification.NotificationListenerService" />
    </intent-filter>
</service>
// 在 IslandService.java 的 onCreate() 中添加：

@Override
public void onCreate() {
    super.onCreate();

    // 检查通知权限
    if (!PermissionHelper.hasNotificationListenerPermission(this)) {
        // 权限未开启，启动引导页面
        Intent intent = new Intent(this, NotificationPermissionActivity.class);
        intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
        startActivity(intent);
        // 注意：这里不要 return，让服务继续启动，但通知功能不工作
        // 用户开启权限后会重新检测
    }

    // 原有代码...
    initWindowManager();
    initSensors();
    createContainer();
    addContainerToWindow();
}
